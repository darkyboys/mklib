show_logs (true);

output_directory (lib);
output (example);
extension (js);
source_path (example);
include (main);
include (math);
include (string);
append (# python is here);
import (example/j.py);

make ();

output (getstdFetch);
extension (js);

include (main);
include (main);
include (main);
include (main);
include (math);
# include (string);

make ();